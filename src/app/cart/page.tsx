import CartClient from "./cart-client";

export const metadata = { title: "Your Cart | BabaG7Star", description: "Review your cart, apply promo codes and proceed to secure checkout." };

export default function Page() {
  return <CartClient />;
}
